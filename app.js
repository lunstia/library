let myLibrary = [];

function Book(title, author, description, image, hasRead) {
  this.title = title;
  this.author = author;
  this.description = description;
  this.image = image;
  this.hasRead = hasRead;
}

Book.prototype.setRead = function(hasRead) {
    self.hasRead = hasRead;
}

Book.prototype.Get

function addBookToLibrary() {
  // do stuff here
}